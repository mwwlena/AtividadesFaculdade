#include <stdio.h>
#include <string.h>

int main () {

    char n [25];

printf("Digite um nome:\n");
    scanf("%s", n);
 
printf("As 4 primeiras letras sao:");
    for (int i = 0; i < 4; i++) {
        printf("%c", n[i]);
    }

return 0;
}
