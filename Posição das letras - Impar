#include <stdio.h>
#include <string.h>

int main () {

    char n [25];

printf("Digite um nome:\n");
    scanf("%s", n);
 
printf("As letras na posicao par sao:");
    for (int i = 0; i < strlen(n); i++) {
        if (i % 2 ==0)
printf("%c", n[i]);
    }

return 0;
}
